package com.example.project001.data

data class LoginResponse(
    // Define properties of your login response
    val token: String // Assuming token is returned in the response
    // Add more properties as per your API response
)
